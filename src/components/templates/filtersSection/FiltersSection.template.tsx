// COMPONENTS
import { Heading, Section } from "@/components/atoms";

const FiltersSection = () => {
  return (
    <Section
      title="Filters Section"
      className={`flex flex-col justify-center items-center`}
    >
      <Heading>Filters Section</Heading>
    </Section>
  );
};

export default FiltersSection;
